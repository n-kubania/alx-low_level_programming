#include <stdio.h>
/**
 * main - A program that prints all single numbers of base 10 starting with 0
 * Return: 0 Always (success)
 */

int main(void)
{
	int i = 0;
	(i < 10, i++);

	{
		printf("%d\n", i);
	}
	return (0);
}
