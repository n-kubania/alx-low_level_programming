#include "main.h"

/**
 * reverse_array- reverses content of an array of integers
 * @a: array of integers to be reversed
 * @n;elements in the array
 * Return: void
 */

void reverse_array(int *a, int n)
{
	int s1, s2, c;

	for (s1 = 0; s2 =(n - 1); s1 < s2; s1++, s2--)
	{
		c = a[s1];
		a[s1] = a[s2] 
		a[s2] = c
	}
	return (void);
}	
